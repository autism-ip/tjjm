# ============================================================
# 实验层配置
# 面向竞赛交付的健康统计、合成异常敏感性与 ablation 汇总
# ============================================================

experiments:
  output_dir: "./outputs/experiments"
  health:
    percentiles:
      - 50
      - 75
      - 90
      - 95
  synthetic:
    radii:
      - 4
      - 6
      - 8
    intensities:
      - 0.5
      - 1.0
